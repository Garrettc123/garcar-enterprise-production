name: Revenue Loop

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  run-revenue:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install pandas scikit-learn joblib
      - run: python revenue_loop.py
      - name: Deploy billing
        run: echo "Live revenue processed"